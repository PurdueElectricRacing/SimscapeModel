function y = vcu_step(p,f,x,y)
    % determine VCU mode
    y = get_VCU_mode();
    
    % get baseline mode

    % if available, get equal torque (ET)

    % if variable torque (VT) available, select control mode from dashboard
end